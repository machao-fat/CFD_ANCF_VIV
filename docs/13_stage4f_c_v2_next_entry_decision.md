# Stage 4F-C-v2 下一阶段进入决定

当前不得进入完整 A/B/C、五切片、九切片或长时间 VIV。

下一授权点是创建新的独立诊断阶段，先冻结并验证 OpenFOAM motion consumed marker 对 `0.000625 s` 网格的时间序列化精度，使 `1.5081250000000002 s` 不被写成 `1.50813 s`。该修复必须限定在证据/桥接时间表示，不得放宽时间身份、Cd、速度、CFL、虚功或力转换 Gate。

即使 bridge 精度修复后能够执行 D2，D1 已显示 Cd 与 predictor/committed 速度差随步数快速放大。后续仍需判断显式弱耦合策略和父初始瞬态是否适用于这一窗口；不得据此自行修改物理参数、几何、域、边界、网格或 Gate。
