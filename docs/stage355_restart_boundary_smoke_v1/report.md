# Stage 355 restart-boundary Smoke

This launcher is authorized for one real 40-step Smoke only: global step
16000 to 16040, time 80.0 s to 80.2 s, with `dt=0.005 s` and three slices.
It uses the Stage354 fresh boundary-aligned candidate. It never starts a
continuation automatically; a failed Gate is terminal for this runtime.
