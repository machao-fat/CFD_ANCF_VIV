# Stage 270 preCICE participant 生命周期合同

## 结论

`STAGE4F_D_PRECICE_PARTICIPANT_CONTRACT_V1_GATE: pass`

本阶段只用 mock backend 验证 participant 生命周期：

`CREATED -> INITIALIZED -> write_displacement -> advance -> read_force -> FINALIZED`

同时验证重复写入、错误 step、未完成 finalize、身份不一致、time/tick 不一致和 kind 不一致均 fail-closed。

专项测试共 11/11 通过，compileall 通过。本阶段 MATLAB、OpenFOAM、WSL CFD 和 CFD participant 启动数均为 0。

## 代码范围

新增 `src/coupling/precice_adapter_v1/participant.py`，它是后端无关的严格生命周期包装器。真实 preCICE backend 尚未接入，避免把 mock 通过误认为真实耦合通过。

Stage 269 的 adapter 构建、旧文件通信、ANCF/EB 核心、物理参数和 Stage 1--268 证据均未修改。正式 Strouhal、稳定 VIV 和锁定区状态继续为 `not_completed`。

下一步需要在全新 runtime 创建真实单切片 preCICE smoke 合同；在获得明确授权前不启动任何真实 solver。
