# CFD ANCF VIV C++ Worker Persistent IPC V1 最终报告

## 结论

最终 Gate：`STAGE4F_D_CPP_WORKER_PERSISTENT_IPC_V1_FINAL_GATE: pass`

独立 C++ ANCF worker 已实现为常驻进程。一个 segment 内 worker 启动 1 次，并通过 framed binary stdin/stdout persistent IPC 连续处理 40 个 step。三 slice 保持 global barrier，checkpoint、force snapshot、身份审计和 fail-closed 语义保留。

## 证据

- 离线 Gate：`STAGE4F_D_CPP_WORKER_PERSISTENT_IPC_V1_OFFLINE_GATE: pass`
- MATLAB/C++ 数值 Gate：`STAGE4F_D_CPP_WORKER_NUMERICAL_EQUIVALENCE_BEFORE_CFD_V1_GATE: pass`
- 真实 confirm Gate：`STAGE4F_D_CPP_WORKER_PERSISTENT_IPC_V1_CONFIRM_GATE: pass`
- 真实 confirm：40/40 physical committed，40/40 fully audited
- C++ worker：启动 1 次
- OpenFOAM：启动 3 次
- WSL：启动 3 次
- MATLAB：0 次
- owned residual：0
- 19/19 IPC 故障注入 fail-closed
- MATLAB worker baseline：44/44 文件保护通过，manifest SHA-256 为 `9b6fcbf48277d07043818cad1e7c2c8cdbc37a80ec71f4c27bd7ab4c8f8331cb`

## 性能

真实 confirm wall-clock 为 `21.0860833 s`。相对 `35.4478716 s` 基线加速 `1.6811x`，相对 `37.1570657 s` 基线加速 `1.7622x`。测得最大阶段为 `T_exchange`，均值 `0.531472 s`；`T_openfoam` 均值 `0.310398 s`；`T_ancf` 均值 `0.004191 s`；`T_sync_and_audit` 均值 `0.005777 s`。

### C++ worker 内部离线 profiling

使用新的独立 profiling run `cpp_worker_persistent_ipc_profiling_001`，仅回放只读 MATLAB fixture，40 steps，未启动任何真实 MATLAB、OpenFOAM、WSL 或 CFD。segment wall-clock 为 `0.053445 s`，worker 启动 1 次，owned residual 为 0。单步总耗时平均 `0.001202503 s`，P50 `0.0011035 s`，P95 `0.0014506 s`；persistent IPC 收发、解码平均 `0.001129825 s`，P50 `0.0010358 s`，P95 `0.0013655 s`。

内核内部实测平均值为：矩阵组装 `0.000281170 s`（P95 `0.0002947 s`），线性求解 `0.000130468 s`（P95 `0.0001464 s`），状态更新 `0.000000045 s`，predictor `0.000000238 s`，external mapping `0.000001088 s`。因此该离线 worker 的主要可测成本是 persistent IPC/序列化；ANCF 内部最大项是矩阵组装。内部项是嵌套计时，不可简单相加为 step wall-clock。原始记录和机器可读审计见 `cpp_worker_internal_profile_audit.json`。

## 数值与保护

MATLAB/C++ 双算在既定工程误差合同下 40/40 通过；严格 bitwise 比较不作为验收条件。ANCF/EB 核心、物理参数、global dt、三 slice 配置、数值阈值、统计门槛、正式 0.2.1 协议、旧证据和旧 runtime 均未修改。

此前 confirm_009 的 NaN 根因是 raw force 被直接送入 correction。confirm_010 已恢复正式因果载荷稳定语义：当前 correction 使用已提交 applied force，当前 raw force 只生成下一 step 的 applied force。

## 范围状态

## 工具与技能审计

实际使用：`cfd-ancf-viv-cpp-worker-audit` skill；CMake 3.31.6；MSVC 2022 BuildTools 14.44 x64；Python `compileall`、`unittest` 和离线 fixture benchmark。当前环境没有单独列出的 CMake/low-level、static-analysis、VTune/AMD uProf 或 QE 专用 skill，因此未声称使用这些外部 skill；本次 profile 采用 C++ `steady_clock` 和持久 IPC 实测，不以理论估算替代测量。

profiling 代码仅增加诊断字段和可选 JSONL 输出，未改变 ANCF 数值路径、协议字段或物理合同。旧证据和旧 runtime 仍按 baseline manifest 保护。

本目标不包含进程内紧耦合。未启动 Stage75、E5-B、E5-C、五 slice、九 slice、长时 VIV、锁定区或实验验证。正式统计继续保持：

`FORMAL_STROUHAL_STATUS=not_completed`

`STABLE_VIV_RESPONSE_CLAIM=not_completed`

`LOCK_IN_CLAIM=not_completed`

`FIVE_SLICE_ENTRY_RECOMMENDATION=do_not_enter`

`NINE_SLICE_ENTRY_RECOMMENDATION=do_not_enter`

`LONG_TIME_VIV_ENTRY_RECOMMENDATION=do_not_enter`

`EXPERIMENTAL_VALIDATION_CLAIM=not_completed`
