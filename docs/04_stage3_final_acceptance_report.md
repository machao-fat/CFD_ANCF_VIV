# 阶段三修复后最终验收报告（当前快照）

## 总结判断

截至当前执行快照，阶段三为：**条件通过基础设施，物理准入未通过，禁止进入多切片**。

## 已通过或条件通过

- 阶段二固定圆柱、扩大域、near-shedding medium/fine、Euler/backward 证据已审计并冻结，不重复计算。
- `ancfFileMotion` 在修复标准字典头和初始确认后，严格同初场 A/B 401 步通过：时间/轨迹误差为 0，Fy 相对 RMSE `1.08e-11`，网格点数值差为 0，U/p 数值差约 `1e-11/1e-9`。
- 持久 EB/ANCF runner、predictor 回滚、checkpoint 和张力/残差自动回归通过。
- 能量审计代码已实现，旧 1000 步结果已按新定义重算但标记为显式储能字段缺失。
- Ur=5.2 短筛查证明释放后非零响应：`A/D_RMS=0.0472`、`f/fn=1.09`，但只有 0.96 周期。

## 未通过或未完成

- 严格 A/B 的中途 CFD restart 自动等价尚未完成。
- SDOF 五 Ur 稳定锁定趋势、公开基准对照和 dt/2 尚未完成；Ur=5.2 长窗口正在执行。
- EB/ANCF 有物理幅值的同工况在线比较尚未完成；旧近零位移结果不采信。
- 完整新字段 1000 步 EB/ANCF 能量平衡、长时间网格质量和两档物理时间步收敛尚未齐全。

## 弱耦合/Aitken

当前不能宣布弱耦合足够，也没有证据证明必须立即实现 Aitken。严格 A/B 已排除接口运动误差；但 SDOF 的窗口能量净注入和结构/流体长期收支尚未完成。若长窗口出现无界响应、残差增长或能量误差不随 dt 降低，下一步进入 CFD/结构 checkpoint 固定松弛和矢量 Aitken；在证据出现前不为增加工作量强行实现。

## 阶段四决定

不具备进入多切片、整根柔性立管、曲线立管或机器学习的资格。单切片结果只能作为接口/结构分支诊断，不能宣称整根立管 VIV。
## 最新补算覆盖（2026-08-04）

本轮执行后的正式结论仍为：**阶段三不通过；基础设施/接口条件通过，物理准入条件不通过；禁止进入多切片。**

已确认的硬证据：

- native 与 `ancfFileMotion` 在相同初始流场、同一网格和同一运动轨迹下完成 401 步到 1 s；时间和轨迹误差为 0，网格点最大差为 0，Fy 相对 RMSE 为 `1.08e-11`。旧 `5.55%` 差异来自非严格/未正确启用动网格的旧案例，已不再采用。
- EB/ANCF 持久 runner、predictor 回滚、checkpoint 和 MATLAB 契约测试通过；张力和 Newton 初始/最终/相对残差字段已修正。
- 能量审计已按 `W_CFD=Σ(F·v_pred)dt`、`W_structure=Σ(F·v_corr)dt` 和耦合缺陷分开实现；但短窗口初始校正瞬态仍主导 EB/ANCF 全窗口能量平衡，不能作为长时间物理准入。

未通过的硬门槛：

- SDOF 仅完成 `Ur=5.2`、`6.0` 的筛查；`Ur=5.2` 长算例在 18 s、约 1.73 周期时因未稳定而停止，未形成五点曲线和每点至少10个稳定周期。
- 同初始流场 dt/2 比较有效，但仅 0.96 周期；位移 RMS、力 RMS、平均功率变化分别为 `6.25%`、`16.03%`、`31.64%`，未通过收敛目标。
- EB/ANCF 100步在线比较响应仍为 `1e-10 m` 量级；可见响应尝试在 `CFL=7.75`、力 `O(1e7 N)` 时安全停止，故未获得可接受的同工况物理比较。
- 严格 native/file 的中断后自动重启逐步等价尚未完成；Aitken 目前只有有界矢量模块和单元测试，尚未集成完整 CFD checkpoint/rollback 强耦合。

因此弱耦合不能宣称“已足够”。当前应优先完成稳态统计窗口、可见但稳定的 EB/ANCF 同工况案例和完整 restart；若残差或能量缺陷仍随时间步不收敛，再进入 CFD/结构固定点与 Aitken 强耦合诊断。阶段三未完成前不进入多切片、整根立管、曲线立管或机器学习。
