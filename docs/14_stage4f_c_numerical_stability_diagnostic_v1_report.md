# Stage 4F-C 数值稳定性诊断 v1

本阶段未启动真实 CFD。离线诊断复核 repair2 与 D1 原始证据，未修改冻结合同、旧结果或生产代码。

repair2 在 step 2 首次同时越过 `|Cd|<=10` 与速度一致性 `<=0.01`；D1 使用更小 `dt=0.00125 s` 后仍从 step 2 开始交替放大，最终 `max |Cd|=346.65279016444276`、速度误差 `0.33929320974259647`。诊断分类为早期显式弱耦合放大，不支持将问题归因于单纯时间步过大。

compileall 与专项测试 3/3 通过。旧证据 hash 已登记且未改变。阶段 Gate 为 `diagnostic_passed_numerical_acceptance_blocked`，下一授权点仅为离线耦合稳定化设计评审，不得自动进入 A/B/C 或真实 CFD。
