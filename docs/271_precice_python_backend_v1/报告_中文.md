# Stage 271 pyprecice backend 离线实现

## 结论

`STAGE4F_D_PRECICE_PYTHON_BACKEND_V1_GATE: pass`

新增 `PrecicePythonBackend`，使用 pyprecice 3.x 的标准 API：`Participant`、`set_mesh_vertices`、`write_data`、`advance`、`read_data`、`finalize`。构造阶段不建立连接，只有显式调用 `initialize()` 才会创建 participant。

本阶段通过注入 fake participant 验证完整调用顺序、数据读写、配置 XML 校验、缺失 displacement、后端断连和 finalize 清理。专项测试 14/14 通过，compileall 通过。

真实 MATLAB、OpenFOAM、WSL、CFD 和 preCICE participant 启动数均为 0。没有生成或运行真实耦合算例。

## 保护状态

Stage 1--270 证据、旧文件通信、ANCF/EB 核心、物理参数和正式统计状态未修改。下一步仍需单独准备 preCICE XML、OpenFOAM adapter 字典和 ANCF participant 配置，再申请一次全新 runtime 的单切片 smoke；不得自动进入三切片或长时 VIV。
